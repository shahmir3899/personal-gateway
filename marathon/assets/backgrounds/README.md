`cabin.jpg` is the widest of the 3 empty-cabinet photos you supplied (5 bays
visible). The other two crops are kept in `../reference/` as alternates if
this framing doesn't work well once real trophies are composited over it —
tighter crops might read better on mobile where the room is displayed taller
(see the `@media (max-width: 560px)` aspect-ratio in `src/ctr.css`).

If this file is ever removed, the widget falls back to the `--ctr-wood-dark`
CSS background color so the layout still renders without it.
