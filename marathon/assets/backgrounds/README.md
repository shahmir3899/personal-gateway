Drop the empty cabin room background photo here as `cabin.jpg` (or update the
filename referenced in `src/ctr-view.js`). Until it's added, the widget falls
back to the `--ctr-wood-dark` CSS background color so the layout still renders.
